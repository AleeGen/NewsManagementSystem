spring:
  liquibase:
    enabled: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://users-db:5432/users
    username: postgres
    password: postgres
  cache:
    type: redis
  data:
    redis:
      host: redis
      port: 6379
ru:
  clevertec:
    cache:
      enabled: false